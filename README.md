# Messaging-app-using-firestore-and-firebase-auth

To get started, use CMD in directory and type "npm install". Also add your own firebase project in the firebase-config.js since I am deleting that one
